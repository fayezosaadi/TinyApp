  <!DOCTYPE html>
  <html lang="en">
    <head>
      <title>TinyApp</title>
    </head>
    <body>
      <header>
      <% if (user && user.email) { %>
        <p>You are logged in as: <%=user.email%><p>
        <form action="/logout" method="GET" ><button>Logout</button>
        </form>
      <% } %>
      </header>

      <form action="/login" method="get" >
        <button>Login</button>
      </form>
      <form action="/register" method="get" >
        <button>register</button>
      </form>
