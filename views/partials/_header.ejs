  <!DOCTYPE html>
  <html lang="en">
    <head>
      <title>TinyApp</title>
    </head>
    <body>
      <header>
      <% if (user && user.email) { %>
        <p>You are logged in as: <%=user.email%><p>
        <form action="/logout" method="GET" >
          <button>Logout</button>
        </form>
         <div>
          <a href="/urls">Mylinks</a>
         </div>
      <% }else{ %>
        <div>
          <a href="/login">Login</a>
        </div>
        <div>
          <a href="/register">Register</a>
        </div>
        <% } %>
      </header>
